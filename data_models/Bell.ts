class Bell {
    Id: number;
    Name: string;
    SoundPath: string;

    constructor(id: number, name: string, soundPath: string) {
        this.Id = id;
        this.Name = name;
        this.SoundPath = soundPath;
    }
}

export default Bell;