class Bell {
    id: number;
    name: string;
    soundPath: string;

    constructor(id: number, name: string, sound: string) {
        this.id = id;
        this.name = name;
        this.soundPath = sound;
    }
}

export default Bell;