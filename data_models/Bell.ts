class Bell {
    id: number;
    name: string;
    sound: Blob;

    constructor(id: number, name: string, sound: Blob) {
        this.id = id;
        this.name = name;
        this.sound = sound;
    }
}